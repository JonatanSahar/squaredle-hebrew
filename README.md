# Squaredle Hebrew

Hebrew-language Squaredle clone for mobile browsers, built as a static site with offline-generated daily puzzles. To run the generator once it exists, use `uv run squaredle-gen`. To serve the site locally, run `python -m http.server -d site`.
